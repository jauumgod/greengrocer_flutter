class ItemModel {
  String itemName, imgURL, unit, description;
  double price;

  ItemModel({
    required this.description,
    required this.imgURL,
    required this.itemName,
    required this.price,
    required this.unit,
  });
}
